import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const StrippedButton = styled(Typography)`
  background: transparent;
  border-style: none;
  font-size: 16px;
  cursor: pointer;
`;
